Algoritmo "soma de quatro numeros"

Var
soma, numero, contador : inteiro

Inicio
para contador de 1 ate 4 faca
     escreval("Digite um número: ")
     leia(numero)
     soma <- soma + numero
fimpara

escreval("A soma é", soma)

Fimalgoritmo