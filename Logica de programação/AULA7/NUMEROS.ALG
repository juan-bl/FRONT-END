Algoritmo "numeros"

Var
i : inteiro
numeros : vetor[1..5] de inteiro

Inicio

para i de 1 ate 5 faca
     escreval("Digite um número: ")
     leia(numeros[i])
fimpara

limpaTela()

enquanto (i > 0) faca
     escreval(numeros[i])
     i <- i - 1
fimenquanto

Fimalgoritmo